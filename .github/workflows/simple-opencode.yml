name: Simple OpenCode Test

on:
  push:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/opencode-run.yml
    with:
      review-name: simple-test
      prompt-file: .opencode/commands/simple-test.md
      timeout-minutes: 5
      use-cache: false
      opencode-model: "opencode/big-pickle"