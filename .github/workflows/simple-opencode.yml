name: Simple OpenCode Test

on:
  push:
  workflow_dispatch:

jobs:
  test:
    uses: ./.github/workflows/opencode-run.yml
    with:
      run-name: simple-test
      prompt-file: .opencode/commands/simple-test.md
      timeout-minutes: 5
      model: "opencode/big-pickle"